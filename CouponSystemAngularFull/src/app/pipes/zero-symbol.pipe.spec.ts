import { ZeroSymbolPipe } from './zero-symbol.pipe';

describe('ZeroSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ZeroSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
